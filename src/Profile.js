import styled from "styled-components";

const ProfileWrapper = styled.div`
  position: absolute;
  top: 10px;
  right: 10px;
`;

export const Profile = () => {
  return (
    <ProfileWrapper>
    </ProfileWrapper>
  );
};
